{
  "name": "dfab-engineering-ballooning",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "start": "python -m uvicorn backend.app.main:app --host 0.0.0.0 --port 8000",
    "verify": "python scripts/verify_vercel_layout.py"
  }
}
